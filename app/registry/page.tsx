import { Section } from "@/components/Section";
export default function RegistryPage() { return <Section eyebrow="Registry" title="Radar Registry"><p className="max-w-3xl leading-8">Every signal tracked by AndyAI Radar receives a structured record: source, date, stack layer, business value, risk score, permission profile, decision category, keywords and next action.</p></Section>; }
